cd devsecops-app

docker info

minikube start --driver=docker

minikube status


docker stop backend

docker rm -f backend

docker build -t devsecops-backend:1.0 .

docker images

docker run -d -p 5000:5000 --name backend devsecops-backend:1.0

docker logs backend

curl http://localhost:5000


